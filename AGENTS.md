# Disc With Friends

Read docs/DESIGN.md before changing mechanics or expanding scope. Mechanics first; do not add elaborate content until the user accepts the feel. Keep the direction/bank/power input contract. Keep simulation free of renderer and DOM dependencies. Run npm test for physics/input changes and meaningful browser playtests for the full throw loop. Separate automated playback from human feel feedback in reports.

Keep private/ ignored. It contains the original design conversation for local continuity. Read it when historical context is required; do not publish it. Public documentation contains the accepted design and roadmap.

Inherits C:\GPT_DEV\AGENTS.md: own repo, public GitHub Pages deployment, push and verify the public URL before calling a shipment ready. Do not advance to course production without mechanics approval.
